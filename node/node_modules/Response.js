module.exports = function(){
	var path = require('path');

	var options = {
  host: 'www.google.com',
  port: 80,
  path: '/upload',
  method: 'POST'
};

var req = http.request(options, function(res) {
  console.log('STATUS: ' + res.statusCode);
  console.log('HEADERS: ' + JSON.stringify(res.headers));
  res.setEncoding('utf8');
  res.on('data', function (chunk) {
    console.log('BODY: ' + chunk);
  });
});
	
	
	var weather = function(req, res){
		var location =  req.body.place
		var days = req.body.days



		var url = "http://api.worldweatheronline.com/free/v1/weather.ashx?q=" + location + "&format=json&num_of_days=" + days + "&key=337b499c0cb420a986a9b044842e69d474ed4d96"
		http.get(url, function(res) {
		  console.log("Got response: " + res.statusCode);

		  res.on("data", function(chunk) {
		    console.log("BODY: " + chunk);
		  });
		}).on('error', function(e) {
		  console.log("Got error: " + e.message);
		});
	}

  	return {Weather: weather};

}